package coda.util

public interface Stack<T> {
	
	/**
	 * Returns true if the stack is empty.
	 */
	public boolean isEmpty()
	
	/**
	 * Pushes the given item on the stack.
	 * @param item
	 */
	public void push(T item)
	
	/**
	 * Pops the highest item off the stack.
	 */
	public T pop()
	
}